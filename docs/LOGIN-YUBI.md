# System admin login

Required: operator name + password (and bound X for /admin session checks that still call requireAdminXSession).
Optional: YubiKey. Banner: YubiKey IS HIGHLY SUGG3ST3D (yellow).
Yubi must not block unlock.
