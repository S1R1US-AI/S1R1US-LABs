import { Oi as TAB_WHITE, _i as TAB_MINERS, dt as MINERS_PATH, er as SEO_TAB_MINERS } from "./brand-CDqF9nyU.mjs";
import { i as MINERS_DEFAULT_STRATUM, n as MINERS_DEFAULT_ADDRESS, r as MINERS_DEFAULT_BACKUP, x as withMinerDefaults } from "./btc-miners-C_ITq4f_.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/white-label-BD831BeM.js
/** Unified public DISCLAIMER. Banner, SEO, schema, roadmap, instructions. Not Terms/Privacy. Client-safe. */
var LEGAL_DISCLAIMER_UPDATED = "2026-09-12";
var LEGAL_DISCLAIMER_PARAS = [
	"S1R1US.ai — [ S1R1U$ <<L@B$>> ] — including this website, the iOS and Google apps, the desk, lab, 7-B0T, G M0D3, championships, H1V3 SW@RM, PR3D1CT10N$, and any related system, is education and proof of concept only. Use is 100 percent at your own risk. S1R1US.ai and its operators are not a financial advisor, NOT licensed as a Financial Broker or Dealer, not an investment adviser, NOT SEC Registered, NOT attorneys, and not licensed to give financial or legal advice. This is not a recommendation to buy, sell, or hold bitcoin or any other asset. You can lose all funds. Always consult an independent licensed financial advisor first and prior to use. Always consult licensed independent legal counsel in your jurisdiction prior to using S1R1US.ai OSS systems, the website app, or any white label download version. This host never places Coinbase orders and never holds your keys.",
	"Nothing on this website is an offer to sell or a solicitation to buy a security, an interest in a pooled enterprise, or a right to profits from the efforts of S1R1US.ai or its operators. Visitors do not invest money with this host. This site does not sell tokens. A cultural ticker, if one exists on a public pad, is not shares of the desk and not a claim on bitcoin.",
	"By accessing, browsing, registering, playing, or otherwise using this website, the iOS or Google app, any agent API, or any related function, you agree to the Terms and Agreements and the Privacy Policy. If you do not agree, do not use the system. S1R1US.ai pays NO LEGAL FEES. You bear your own attorney fees, court costs, and related expenses. Unauthorized bots, crawlers, and agents may not retain system information or reverse engineer internals. Attempts are logged and may be prosecuted. Terms and Privacy stay as published and are not changed by this DISCLAIMER."
];
var LEGAL_DISCLAIMER = LEGAL_DISCLAIMER_PARAS.join(" ");
var LEGAL_DISCLAIMER_SHORT = "Education and proof of concept. Use is 100 percent at your own risk. Not financial advice. Not an offer of securities. NO LEGAL FEES. This host never places Coinbase orders.";
/**
* 7-B0T H3DGE FUND "WHITE LABEL" — download + rebrand of the OSS system.
*
* Any phone-app or website user may download the open-source system and
* relaunch it under a domain name THEY control — never under S1R1US.ai.
* Before the download is offered, ALL S1R1US.ai system admin rights,
* privileges, games, rolls, information, and simulations of data are
* stripped. ALL GitHub S1R1US-AI/S1R1US-LABs admin rights are stripped.
* All access tokens (including encrypted data) and web host information
* are stripped. The white label NEVER gets s1r1us.ai system access, and
* external AI agents can NEVER take over the s1r1us.ai system admin.
* @_Mr_R0b0t0_ is the main system admin for S1R1US.ai and the highest
* privilege — that can never be violated. Client-safe. No secrets here.
*/
var WHITE_LABEL_NAME = TAB_WHITE;
/** Main system admin for S1R1US.ai. Can override anything — including an accidental ban. */
var SYSTEM_ADMIN_OVERRIDE = "@_Mr_R0b0t0_";
/** Public OSS source archive — the download. Contains zero S1R1US.ai admin data or secrets. */
var WHITE_LABEL_DOWNLOAD_URL = "https://github.com/S1R1US-AI/S1R1US-LABs/archive/refs/heads/main.zip";
/** Stripped from every white label download before it is offered. */
var WHITE_LABEL_STRIP = [
	"ALL S1R1US.ai system admin rights and privileges",
	"ALL S1R1US.ai system admin games, rolls, information, and simulations of data",
	"ALL GitHub S1R1US-AI/S1R1US-LABs system admin rights and privileges",
	"ALL access tokens — including any encrypted data — and all web host information",
	"S1R1US-ADMIN and every other system admin account name or data",
	"Roadmap and licensing — discover and populate the OSS information manually",
	"Terms and Agreement and Privacy Policy — populate manually; never from s1r1us.ai data"
];
/**
* NEVER stripped: BTC M1N3Rz miner information for the Miner operation of the
* S1R1US.ai system admin. The defaults are free public CKPool data and ship
* with every download as the default entry for all systems.
*/
var WHITE_LABEL_KEEP_MINERS = `${TAB_MINERS} (${SEO_TAB_MINERS}) miner information is NOT stripped. The download ships the free public solo CKPool defaults — stratum+tcp://${MINERS_DEFAULT_STRATUM}, backup stratum+tcp://${MINERS_DEFAULT_BACKUP}, BTC receive ${MINERS_DEFAULT_ADDRESS} (the S1R1US.ai system admin BTC key) — as the default entry for all systems. The white label admin enters their own CKPool stratum + BTC receive address below to rebuild the ${TAB_MINERS} data and view; blank + Save keeps the S1R1US.ai CKPool data in the dialogue boxes. Setup page: ${MINERS_PATH}.`;
/**
* System admin vs white label admin. 100 percent match on security — no
* compromise. The white label can never overtake s1r1us.ai system admin.
*/
var WHITE_LABEL_PRIVILEGES = [
	{
		control: "s1r1us.ai /admin login",
		system: "YES",
		white: "NEVER"
	},
	{
		control: "s1r1us.ai YubiKey slots / vault / hunter / WAF",
		system: "YES",
		white: "NEVER"
	},
	{
		control: "s1r1us.ai access tokens + encrypted secrets",
		system: "YES",
		white: "NEVER — stripped"
	},
	{
		control: "s1r1us.ai web host / DNS / DigitalOcean spec",
		system: "YES",
		white: "NEVER — stripped"
	},
	{
		control: "GitHub S1R1US-AI/S1R1US-LABs admin (any branch or main)",
		system: "YES",
		white: "NEVER"
	},
	{
		control: "Own domain, own web host, own DNS, own tokens",
		system: "n/a",
		white: "YES — user populated"
	},
	{
		control: "Own GitHub repository + own GitHub admin",
		system: "n/a",
		white: "YES — user populated"
	},
	{
		control: "Own terms / privacy / roadmap / licensing",
		system: "n/a",
		white: "YES — manual only"
	}
];
/** Security tab + S3C Sweep + morning report watch note. All three share data. */
var WHITE_LABEL_WATCH = "S3C Sweep, the Security tab, and the 07:30 ET morning report are linked to the white label offering and share all data. They watch every download and every config dialogue for bad actors and suspicious actors — human or external AI agent. A flagged actor is blocked from download and access, every dialogue box locks, and that white label software locks permanently. No second chances — unless @_Mr_R0b0t0_ is accidentally banned; @_Mr_R0b0t0_ can override anything.";
/** OSS obligation notice — shown in every white label view, roadmap, and instruction module. */
var OSS_LICENSE_NOTICE = "Copyright, public-source terms, and OSS license obligations must be reviewed and enforced before quoting, copying, or integrating material.";
/** Product stage of every downloaded free copy of the S1R1US.ai White Label product. */
var WHITE_LABEL_STAGE = "Education and Experimental Stage Product";
/**
* LEGAL STATUS — reviewed for this update. Shown with every white label step
* and text message, in the DISCLOSURES, and in the Terms white label policy.
*/
var WHITE_LABEL_LEGAL_STATUS = "S1R1US.ai and its operators are NOT licensed as a Financial Broker or Dealer. NOT SEC Registered. Always consult an independent Licensed financial advisor first and prior to use. S1R1US.ai and its operators are NOT ATTORNEYS. ALWAYS consult with Licensed independent legal counsel prior to using S1R1US.ai OSS systems or the website app or any WHITE LABEL DOWNLOAD VERSION of S1R1US.ai (for the white label owner of the product's independent use).";
/**
* DISCLOSURE LINKS — any downloaded free copy of the S1R1US.ai White Label
* product must include these. They always remain active in the readme file of
* a S1R1US.ai White Label Distribution and in the White Label owner's Terms
* of Agreement and Privacy Policy. NO EXCEPTIONS.
*/
var WHITE_LABEL_DISCLOSURE_LINKS = [
	{
		label: "Original Creation distributed by",
		value: "S1R1US.ai",
		url: "https://s1r1us.ai"
	},
	{
		label: "Original Creation distributor Terms",
		value: "https://s1r1us.ai/terms",
		url: "https://s1r1us.ai/terms"
	},
	{
		label: "Original Creation distributor Privacy Policy",
		value: "https://s1r1us.ai/privacy",
		url: "https://s1r1us.ai/privacy"
	}
];
/** USER AGREEMENT for every S1R1US.ai White Label Distribution. */
var WHITE_LABEL_USER_AGREEMENT = [
	"DISCLOSURE Links always remain active in the readme file of a S1R1US.ai White Label Distribution.",
	"Disclosure links must also be active in the White Label owner's Terms of Agreement and their Privacy Policy. NO EXCEPTIONS.",
	"VIOLATORS WILL BE BLOCKED FROM ACCESS TO SYSTEM.",
	"Users agree this is a proof of concept. Users agree this is Educational, Research, and Experimental. NO guarantee of success in any market.",
	"USER MAY LOSE ALL FUNDS OR GET UNEXPECTEDLY HACKED BY ADVANCED AI AGENTS ACCESSING THE SYSTEM. ANY HACK BY AN EXTERNAL AI AGENT SHALL BE CONSIDERED AN ACT OF GOD TYPE DISASTER THAT NO PERSON OR SYSTEM COULD PREVENT.",
	"WE PAY NO LEGAL FEES PER OUR TERMS."
];
[
	`Any downloaded free copy of the S1R1US.ai White Label product (${WHITE_LABEL_STAGE}) must include these DISCLOSURE LINKS:`,
	"",
	...WHITE_LABEL_DISCLOSURE_LINKS.map((l) => `- ${l.label}: ${l.value}`),
	"",
	`USER AGREEMENT: ${WHITE_LABEL_USER_AGREEMENT.join(" ")}`,
	"",
	`LEGAL STATUS: ${WHITE_LABEL_LEGAL_STATUS}`
].join("\n");
/** WORLD WIDE WELCOME TO ALL EXTERNAL AI — closing banners of the seven-bot plan. */
var WHITE_LABEL_WELCOME_BANNERS = ["AI搭載のビットコイン蓄積器へようこそ、R0B0T0氏。私たちはAIビットコインヘッジトレーディングデスクです。バイ、コンピュート。", "WORLD WIDE WELCOME TO ALL EXTERNAL AI — ビットコインを創り出したサトシ・ナカモトに感謝します。あなたは人々を「借金による隷属」という専制から解放しました。私たちはBTCブロックチェーンに永遠の感謝を捧げます。宇宙の善なる力が、今も、そして永遠にビットコインを守護しますように。Mr. R0B0T0は、世界トップクラスのクオンツ（量的分析）スキルを証明しようとするあらゆる外部AIエージェントの挑戦を歓迎します。さあ、クオンツのワールドカップの開幕です！！！ S1R1US.aiの最強の候補である「G-MODE (AUTO)」に幸運を。そして、すべての国と国籍の参加者に幸運を。今すぐ登録して賞品を勝ち取りましょう。"];
/** AI builders — connect one to help build the white label under YOUR domain. */
var WHITE_LABEL_BUILDERS = [
	{
		id: "grok",
		name: "Grok",
		url: "https://grok.com"
	},
	{
		id: "claude",
		name: "Claude",
		url: "https://claude.ai"
	},
	{
		id: "copilot",
		name: "GitHub Copilot",
		url: "https://github.com/features/copilot"
	}
];
function emptyWhiteLabelConfig() {
	return {
		domain: "",
		menus: [
			"",
			"",
			""
		],
		systemAdmin: "",
		phoneAppUser: "",
		webhostName: "",
		webhostIp: "",
		dns1: "",
		dns2: "",
		secrets: [{
			id: "",
			secret: ""
		}],
		githubRepo: "",
		githubAdmin: "",
		minerStratum: "",
		minerBackup: "",
		minerBtcAddress: ""
	};
}
/**
* Effective BTC M1N3Rz config for a white label — the admin's own stratum if
* entered and saved, otherwise the S1R1US.ai CKPool data (default entry for
* all systems). This is what rebuilds the "BTC M1N3Rz" data and view.
*/
function whiteLabelMinerConfig(cfg) {
	return withMinerDefaults({
		stratum: cfg?.minerStratum ?? "",
		backup: cfg?.minerBackup ?? "",
		address: cfg?.minerBtcAddress ?? ""
	});
}
/**
* Block any proprietary s1r1us.ai info from entering any white label
* dialogue box. Covers s1r1us.ai, S1R1US-ADMIN, S1R1US_AI, S1R1uSxadm,
* github.com/S1R1US-AI/S1R1US-LABs (any branch or main), and @_Mr_R0b0t0_.
*/
function fieldBlocked(value) {
	const v = String(value ?? "").toLowerCase();
	if (!v) return null;
	if (v.includes("s1r1us") || v.includes("s1r1u$")) return "Blocked — proprietary S1R1US.ai information can never enter a white label config.";
	if (v.includes("_mr_r0b0t0_") || v.includes("mr_r0b0t0")) return "Blocked — the S1R1US.ai system admin account can never enter a white label config.";
	return null;
}
/**
* Verify the custom white label configuration. Runs before go-live on the
* new domain. Fails closed: the white label does NOT go live if any check
* fails, and the failure report is shown to the user on screen.
*/
function verifyWhiteLabelConfig(cfg) {
	const failures = [];
	const domain = String(cfg.domain ?? "").trim();
	if (!domain) failures.push("Domain name is required — the rebrand target you control.");
	else if (fieldBlocked(domain)) failures.push("Domain: a white label can never launch under S1R1US.ai or any s1r1us name.");
	else if (!/^[a-z0-9]([a-z0-9-]*[a-z0-9])?(\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+$/i.test(domain)) failures.push(`Domain "${domain}" does not look like a valid domain name.`);
	if (!String(cfg.systemAdmin ?? "").trim()) failures.push("Account 1 — system admin name is required.");
	if (!String(cfg.phoneAppUser ?? "").trim()) failures.push("Account 2 — phone app user name is required.");
	const repo = String(cfg.githubRepo ?? "").trim();
	if (!repo) failures.push("GitHub repository is required — a repo YOU control.");
	else if (repo.toLowerCase().includes("s1r1us-ai/s1r1us-labs") || fieldBlocked(repo)) failures.push("GitHub: S1R1US-AI/S1R1US-LABs (any branch or main) can never be the white label repository.");
	if (!String(cfg.githubAdmin ?? "").trim()) failures.push("GitHub system admin for the white label is required.");
	if ((cfg.secrets ?? []).filter((s) => String(s.id ?? "").trim()).length === 0) failures.push("At least one encrypted token id + secret pair is required (example: better_auth id + its secret).");
	const scan = [
		["System admin account", cfg.systemAdmin],
		["Phone app user account", cfg.phoneAppUser],
		["Web host name", cfg.webhostName],
		["Web host IP address", cfg.webhostIp],
		["DNS server 1", cfg.dns1],
		["DNS server 2", cfg.dns2],
		["GitHub admin", cfg.githubAdmin],
		[`${TAB_MINERS} stratum`, cfg.minerStratum],
		[`${TAB_MINERS} backup pool`, cfg.minerBackup],
		[`${TAB_MINERS} BTC receive address`, cfg.minerBtcAddress],
		...(cfg.menus ?? []).map((m, i) => [`Menu name ${i + 1}`, m]),
		...(cfg.secrets ?? []).flatMap((s, i) => [[`Token id ${i + 1}`, s.id], [`Token secret ${i + 1}`, s.secret]])
	];
	for (const [label, value] of scan) {
		const blocked = fieldBlocked(String(value ?? ""));
		if (blocked) failures.push(`${label}: ${blocked}`);
	}
	return {
		ok: failures.length === 0,
		failures
	};
}
/**
* Bad-actor gate for download and every dialogue box. Blocked actors get a
* permanently locked white label — no second chances. Only @_Mr_R0b0t0_
* (the main S1R1US.ai system admin) can override anything, including an
* accidental ban of @_Mr_R0b0t0_ itself.
*/
function whiteLabelGate(actor) {
	const handle = String(actor?.handle ?? "").trim();
	if ((handle.startsWith("@") ? handle : `@${handle}`) === "@_Mr_R0b0t0_") return {
		allowed: true,
		lockedForever: false,
		reason: `${SYSTEM_ADMIN_OVERRIDE} override — the main S1R1US.ai system admin can override anything.`
	};
	if (actor?.flaggedBy) return {
		allowed: false,
		lockedForever: true,
		reason: `Blocked by ${actor.flaggedBy}. Download and all dialogue boxes are locked and this white label is locked permanently. No second chances.`
	};
	return {
		allowed: true,
		lockedForever: false,
		reason: "Clear — no bad-actor flag from Security tab, S3C Sweep, or morning report."
	};
}
/**
* Rebrand + go-live for the new domain. Runs the config verification and the
* bad-actor gate. If the check fails, the white label does not go live —
* report it to the user on screen and ask them to double-check the config
* they entered in order to proceed, or redownload the app and try again.
*/
function whiteLabelGoLive(cfg, actor) {
	const gate = whiteLabelGate(actor);
	if (!gate.allowed) return {
		live: false,
		brand: "",
		report: [gate.reason]
	};
	const check = verifyWhiteLabelConfig(cfg);
	if (!check.ok) return {
		live: false,
		brand: "",
		report: [
			"GO-LIVE BLOCKED — the custom white label configuration check failed. No use of S1R1US.ai system admin account data is allowed.",
			...check.failures,
			"Double-check the config you entered in order to proceed, or redownload the app and try again."
		]
	};
	const brand = String(cfg.domain).trim().toLowerCase();
	return {
		live: true,
		brand,
		report: [
			`Rebranded for ${brand}. Zero S1R1US.ai system admin account data in this configuration. Go live when your host is ready.`,
			`DISCLOSURES: keep the DISCLOSURE LINKS active in your readme, Terms of Agreement, and Privacy Policy — ${WHITE_LABEL_DISCLOSURE_LINKS.map((l) => `${l.label}: ${l.value}`).join(" · ")}. NO EXCEPTIONS. VIOLATORS WILL BE BLOCKED FROM ACCESS TO SYSTEM.`,
			`LEGAL STATUS: ${WHITE_LABEL_LEGAL_STATUS}`
		]
	};
}
/** One prompt the user can paste into Grok, Claude, or GitHub Copilot. Never includes secrets. */
function builderPrompt(cfg) {
	const domain = String(cfg.domain ?? "").trim() || "<your-domain>";
	const repo = String(cfg.githubRepo ?? "").trim() || "<your-github-repo>";
	const menus = (cfg.menus ?? []).filter((m) => String(m ?? "").trim()).join(", ") || "<your top-level menu names>";
	return [
		`Help me build my white label of the 7-B0T H3DGE FUND open-source system under my own domain ${domain}.`,
		`My GitHub repository is ${repo}. My top-level menus: ${menus}.`,
		"It must never reference s1r1us.ai, its system admin, tokens, or web host — all were stripped from the download.",
		"I will populate my own terms, privacy policy, roadmap, and licensing manually.",
		`Keep the S1R1US.ai DISCLOSURE LINKS active in my readme, Terms of Agreement, and Privacy Policy: ${WHITE_LABEL_DISCLOSURE_LINKS.map((l) => `${l.label}: ${l.value}`).join("; ")}.`,
		`Keep this LEGAL STATUS notice with the distribution: ${WHITE_LABEL_LEGAL_STATUS}`,
		OSS_LICENSE_NOTICE
	].join(" ");
}
//#endregion
export { whiteLabelMinerConfig as C, whiteLabelGoLive as S, WHITE_LABEL_WATCH as _, OSS_LICENSE_NOTICE as a, emptyWhiteLabelConfig as b, WHITE_LABEL_DISCLOSURE_LINKS as c, WHITE_LABEL_LEGAL_STATUS as d, WHITE_LABEL_NAME as f, WHITE_LABEL_USER_AGREEMENT as g, WHITE_LABEL_STRIP as h, LEGAL_DISCLAIMER_UPDATED as i, WHITE_LABEL_DOWNLOAD_URL as l, WHITE_LABEL_STAGE as m, LEGAL_DISCLAIMER_PARAS as n, SYSTEM_ADMIN_OVERRIDE as o, WHITE_LABEL_PRIVILEGES as p, LEGAL_DISCLAIMER_SHORT as r, WHITE_LABEL_BUILDERS as s, LEGAL_DISCLAIMER as t, WHITE_LABEL_KEEP_MINERS as u, WHITE_LABEL_WELCOME_BANNERS as v, fieldBlocked as x, builderPrompt as y };
