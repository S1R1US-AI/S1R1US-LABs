import { n as checkpointLabel } from "./checkpoint-Bsdvrfn5.mjs";
//#region ../../workspace/node_modules/.nitro/vite/services/ssr/assets/live-sim-O8yBD7L4.js
/** G M0D3 AUTO + AI agents as-live simulation until go-live. Client-safe. */
var LIVE_SIM_NAME = "G M0D3 AUTO · AI agents live simulation";
var LIVE_SIM_SEO = "GM Mode AUTO live simulation";
var LIVE_SIM_TZ = "America/New_York";
function liveSimNote(status, checkpoint = 68) {
	const tag = checkpointLabel(checkpoint);
	return status === "LIVE" ? `${tag} as-live paper simulation of G M0D3 AUTO and AI agents on live Coinbase last. Data pulls are on. Pause allowed for system Admin and phone-app Admin. Championship World Cup / C@LL 0UT pause is also system Admin and phone-app Admin. This host never places Coinbase orders.` : `${tag} simulation paused (G M0D3 AUTO, AI agents). Last tape held for morning report. Data pulls idle. Continue from Admin Console (system or phone app). Championship World Cup / C@LL 0UT pause is system Admin and phone-app Admin.`;
}
//#endregion
export { liveSimNote as i, LIVE_SIM_SEO as n, LIVE_SIM_TZ as r, LIVE_SIM_NAME as t };
