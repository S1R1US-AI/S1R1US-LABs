# DigitalOcean App Platform — Settings → web → Environment Variables → Edit → Add from .env
# Copy this file, fill the two secrets, then import. Never commit a filled .env.
# After import: Encrypt BETTER_AUTH_SECRET and GROK_AUTH_CLIENT_SECRET.
# Confirm GROK_AUTH_CLIENT_ID is the full string (no ellipsis). No DATABASE_URL (PGLite).

NODE_ENV=production
VITE_AUTH_ENABLED=true
HOST=0.0.0.0
PORT=8080
NITRO_PRESET=node-server
BETTER_AUTH_URL=https://s1r1us.ai
BETTER_AUTH_SECRET=
GROK_AUTH_ISSUER=https://auth.grok.me
GROK_AUTH_CLIENT_ID=grok_a3389f926a0c42b3b1c95fec1287e3ef
GROK_AUTH_CLIENT_SECRET=

# Optional. Server only. Never VITE_. Tape still works without it.
# XAI_API_KEY=
